export { EventType } from '../adapter';
export { SubmitData, StartedData, ClientEvent, ServerEvent, RoomData, JoinData, NextData, StartData, ErrorData } from '../adapter';

export type CreateData = null;